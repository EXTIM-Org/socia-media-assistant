# راهنمای فعال‌سازی ارسال دایرکت خودکار از روی کامنت 🚀

خبر بسیار خوب این است که زیرساخت این قابلیت (هم در بک‌اند و هم در داشبورد مدیریت) از قبل با بهترین حالت ممکن در سیستم شما پیاده‌سازی شده است! 
شما همین الان می‌توانید بدون نیاز به هیچ کدنویسی جدیدی از این قابلیت استفاده کنید.

## معماری فعلی سیستم (چگونه کار می‌کند؟)
سیستم دقیقاً بر اساس همان **پیشنهاد نهایی (کلمات کلیدی + ریپلای عمومی تصادفی + دایرکت)** کار می‌کند.

1. **دریافت کامنت:** وقتی کاربری کامنت می‌گذارد، فیسبوک آن را به سرور ما می‌فرستد.
2. **بررسی کلمات کلیدی:** سرور کلمات کلیدی که شما در داشبورد تعریف کرده‌اید را بررسی می‌کند.
3. **ارسال پیام‌ها:** اگر کلمه‌ای مطابقت داشت، سرور به صورت همزمان:
   - یک پاسخ عمومی (ریپلای) به صورت تصادفی از بین لیست پاسخ‌های شما زیر آن کامنت می‌گذارد.
   - پیام دایرکت اختصاصی شما را به دایرکت آن شخص می‌فرستد.

---

## 🛠 مراحل راه‌اندازی در ۳ قدم ساده:

### ۱. فعال‌سازی دریافت کامنت در پنل متا
برای اینکه فیسبوک کامنت‌ها را به سرور ما بفرستد، باید یک تیک دیگر را هم روشن کنید:
1. وارد پنل توسعه‌دهندگان متا شوید.
2. به مسیر **Messenger > Instagram Settings** بروید.
3. در بخش **Webhooks for Instagram**، روی دکمه **Edit** مقابل پیجتان کلیک کنید.
4. علاوه بر `messages` و `messaging_postbacks`، این بار تیکِ **`comments`** را هم بزنید و Save کنید.

> [!IMPORTANT]
> برای اینکه ربات بتواند هم ریپلای کند و هم دایرکت بفرستد، اکانت فیسبوک متصل شده باید دسترسی مدیریت کامنت‌ها را داشته باشد. 

### ۲. ورود به داشبورد مدیریت
پروژه فرانت‌اند (Next.js) را روی سیستم خودتان اجرا کنید (با دستور `npm run dev` در پوشه frontend).
سپس مرورگر را باز کرده و به آدرس زیر بروید:
`http://localhost:3000/dashboard/bot-settings`

### ۳. تعریف کلمات کلیدی و پیام‌ها
در صفحه تنظیمات ربات، روی تب **«قوانین پاسخگویی»** کلیک کنید و روی دکمه **«قانون جدید»** بزنید.
یک باکس زیبا برای شما باز می‌شود که ۳ بخش دارد:
- **کلمات کلیدی:** (مثلاً بنویسید: `قیمت, خرید, سفارش`)
- **پاسخ دایرکت:** (متنی که می‌خواهید بلافاصله دایرکت شود).
- **پاسخ‌های کامنت:** (چند متن بنویسید و با اینتر جدا کنید، مثلاً "دایرکت شد عزیزم" و "ارسال شد چک کنید". ربات برای اینکه توسط اینستاگرام اسپم شناخته نشود، هربار به صورت شانسی یکی از این‌ها را در کامنت‌ها ریپلای می‌کند).

همه چیز آماده‌ی استفاده است! 🎉
