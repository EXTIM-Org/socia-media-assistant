# راهنمای توسعه لوکال (Local Development)

این راهنما توضیح می‌دهد که چطور می‌توانید پروژه را روی کامپیوتر شخصی خود (لوکال) توسعه دهید، در حالی که از دیتابیس، کش و وب‌هوکِ سرور اصلی استفاده می‌کنید.

## استراتژی تونل ترکیبی (Hybrid Tunneling)
ما برای توسعه لوکال از روش **Port Forwarding SSH** استفاده می‌کنیم. این روش به شما اجازه می‌دهد کدهای بک‌اند را روی سیستم خانه اجرا کنید، اما:
1. دیتابیس Postgres را از سرور بخوانید.
2. دیتابیس Redis را از سرور بخوانید.
3. ترافیک وب‌هوک متا (که به سرور می‌رسد) را مستقیماً به سیستم خانه خود هدایت کنید.

### پیش‌نیازها
- پروژه روی سیستم شما کلون شده باشد.
- وابستگی‌ها (npm install) در پوشه‌های `backend` و `frontend` نصب شده باشند.
- سرویس بک‌اند روی سرور باید متوقف شده باشد تا پورت وب‌هوک آزاد شود. (دستور: `pm2 stop instagram-backend` روی سرور)

### دستور جادویی اتصال (SSH Tunnel)
یک ترمینال در سیستم عامل ویندوز/مک خود باز کنید و دستور زیر را اجرا کنید:

```bash
ssh -L 5432:127.0.0.1:5433 -L 6379:127.0.0.1:6380 -R 3001:localhost:3001 -o ServerAliveInterval=60 -o ServerAliveCountMax=3 root@82.115.21.96 -N
```

**تشریح دستور:**
- `-L 5432:127.0.0.1:5433` : پورت `5433` (Postgres روی سرور) را روی پورت `5432` لوکال شما فوروارد می‌کند.
- `-L 6379:127.0.0.1:6380` : پورت `6380` (Redis روی سرور) را روی پورت `6379` لوکال شما فوروارد می‌کند.
- `-R 3001:localhost:3001` : پورت `3001` سرور را باز کرده و هر درخواستی که (از سمت متا و کلودفلر) به آن پورت بیاید را به پورت `3001` لوکال شما می‌فرستد.
- `-N` : به SSH می‌گوید که نیازی به اجرای کامند یا باز کردن شل نیست، فقط تونل را نگه دار.
- `-o ServerAliveInterval=60 -o ServerAliveCountMax=3` : جلوگیری از قطع شدن اتصال SSH در صورت بیکار بودن (Idle).

### تنظیمات `.env` در محیط لوکال
در سیستم خانه خود، مطمئن شوید که فایل `backend/.env` پورت‌های لوکالِ فوروارد شده را هدف قرار می‌دهد (دقیقاً مشابه چیزی که در دستور SSH فوروارد کردیم):

```env
DATABASE_URL="postgresql://user:password@127.0.0.1:5432/befroosh_db?schema=public"
REDIS_HOST="127.0.0.1"
REDIS_PORT="6379"
```

### اجرای پروژه
بعد از باز کردن تونل بالا (و باز نگه داشتن آن ترمینال):

1. **بک‌اند:** وارد پوشه `backend` شوید و `npm run start:dev` را بزنید. (روی پورت 3001 اجرا می‌شود).
2. **فرانت‌اند:** وارد پوشه `frontend` شوید و `npm run dev` را بزنید.

حالا اگر با گوشی به ربات اینستاگرام خود پیام بدهید، متا پیام را به سرور می‌فرستد، سرور از طریق تونل معکوس آن را به بک‌اندِ روی کامپیوتر شما می‌فرستد، و شما می‌توانید به صورت کاملاً زنده (Live) تغییرات کد را بررسی و دیباگ کنید!
